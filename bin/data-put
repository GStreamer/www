#!/bin/sh
if ! test -d data
then
  echo "Please run this from the www/ module dir"
  exit 1
fi

chmod -R g+w data 2> /dev/null
rsync -arv -e ssh data pdx.freedesktop.org:/home/projects/gstreamer/www
rsync -arv -e ssh data/pkg pdx.freedesktop.org:/home/projects/gstreamer/www/data
